apiVersion: v1
kind: Secret
metadata:
  name: my-secret
type: Opaque
# Example only: never commit real Secret values to git.
# Manage real secrets with External Secrets Operator / Sealed Secrets / SOPS instead.
data:
  password: UExBQ0VIT0xERVI=  # "PLACEHOLDER" (base64)
